import { storiesOf } from "@storybook/react";
import * as React from "react";

import { CriticalError } from "./CriticalError";

storiesOf("CriticalError", module).add("default", () => <CriticalError />);
